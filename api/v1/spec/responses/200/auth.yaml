AuthorizationUrl:
    description: Authorization url and state received from osu!'s API
    content:
        application/json:
            schema:
                $ref: "../../schemas/auth.yaml#/AuthorizationUrl"

JWTClaims:
    description: Claims of a validated JWT
    content:
        application/json:
            schema:
                $ref: "../../schemas/auth.yaml#/JWTClaims"