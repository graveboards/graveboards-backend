ScoreCreate:
    description: A JSON payload containing the data of the score to create
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/score.yaml#/ScoreCreate"