RequestCreate:
    description: A JSON payload containing the data of the beatmapset request to create
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/request.yaml#/RequestCreate"

RequestPatch:
    description: A JSON payload containing the updated data of the beatmapset request
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/request.yaml#/RequestPatch"