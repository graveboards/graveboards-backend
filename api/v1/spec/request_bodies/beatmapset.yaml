BeatmapsetCreate:
    description: A JSON payload to add a beatmapset to or update on the server
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/beatmapset.yaml#/BeatmapsetCreate"