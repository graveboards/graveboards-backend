Beatmapset:
    description: A JSON payload containing a beatmapset to add to or update on the server
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/beatmapset.yaml#/Beatmapset"