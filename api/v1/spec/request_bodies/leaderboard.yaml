LeaderboardCreate:
    description: A JSON payload containing the data of the leaderboard to create
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/leaderboard.yaml#/LeaderboardCreate"

LeaderboardPatch:
    description: A JSON payload containing the updated data of the leaderboard
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/leaderboard.yaml#/LeaderboardPatch"