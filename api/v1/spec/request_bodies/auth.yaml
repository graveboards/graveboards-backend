AuthorizationCode:
    description: URL-encoded form payload containing the authorization code received from the osu! OAuth flow to authenticate with
    required: true
    content:
        application/x-www-form-urlencoded:
            schema:
                $ref: "../schemas/auth.yaml#/AuthorizationCode"