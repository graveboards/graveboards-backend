UserCreate:
    description: A JSON payload containing the data of the user to create
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/user.yaml#/UserCreate"