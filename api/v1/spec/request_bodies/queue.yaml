QueueCreate:
    description: A JSON payload containing the data of the queue to create
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/queue.yaml#/QueueCreate"

QueuePatch:
    description: A JSON payload containing the updated data of the queue
    required: true
    content:
        application/json:
            schema:
                $ref: "../schemas/queue.yaml#/QueuePatch"