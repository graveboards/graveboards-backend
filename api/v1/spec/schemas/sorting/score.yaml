ScoreSorting:
    title: ScoreSorting
    description:
        $ref: "./description.yaml#/SortingDescription"
    type: array
    items:
        type: object
        additionalProperties: false
        properties:
            field:
                $ref: "#/ScoreSortingField"
            order:
                $ref: "./sorting_order.yaml#/SortingOrder"
        required:
            - field

ScoreSortingField:
    description: >
        ### Enum of supported field identifiers for sorting
    type: string
    enum:
        - Score.id