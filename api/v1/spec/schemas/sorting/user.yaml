UserSorting:
    title: UserSorting
    description:
        $ref: "./description.yaml#/SortingDescription"
    type: array
    items:
        type: object
        additionalProperties: false
        properties:
            field:
                $ref: "#/UserSortingField"
            order:
                $ref: "./sorting_order.yaml#/SortingOrder"
        required:
            - field

UserSortingField:
    description: >
        ### Enum of supported field identifiers for sorting
    type: string
    enum:
        - User.id